{% extends "RigauxtNewsBundle::layout.html.twig" %}

{% form_theme form 'RigauxtAlumniBundle:Form:fields.html.twig' %}

{% block title %}{{ parent() }} - Ajouter une News{% endblock %}

{% block rigauxtnews_body %}
	<div class="jumbotron" style="width: 75%; margin: auto">
		<form action="{{ path('rigauxt_news_add') }}" method="post">
			<fieldset>
				<legend>Nouvelle News</legend>
				<div class="form-group" style="margin-bottom: 0">
					{% if form_errors(form) %}
						<div class="alert alert-warning">
							{{ form_errors(form) }}
						</div>
					{% endif %}
					{{ form_widget(form.titre, {'attr': {'placeHolder': 'Titre'}}) }}
					{{ form_widget(form.contenu) }}
					<button type="submit" class="btn btn-success" style="width: 100%">Publier</button>
					{{ form_rest(form) }}
				</div>
			</fieldset>
		</form>
	</div>
{% endblock %}
